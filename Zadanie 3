# Zadanie 3 Napisz funkcję, która posortuje listę liczb w porządku niemalejącym.

# sortowanie liczb w porządku niemalejącym

def sortowanie (argumenty):
    sort=sorted(argumenty)
    return sort

#przykład

x = [1,3, 5, 3,1, 9]

sort=sorted(x)
print (sort)
